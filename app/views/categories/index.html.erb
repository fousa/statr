<h2>List categories</h2>

<ul>
  <% @categories.each do |category| -%>
    <li><%= category.name %> (<%= link_to "edit", edit_category_path(category) %> - <%= link_to "remove", category, :method => :delete, :confirm => "Are you sure?" %>)</li>
  <% end -%>
</ul>
